{% extends 'base.html.twig' %}

{% block title %}Dom{% endblock %}

{% block body %}
    <h1>Dom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dom.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ dom.type }}</td>
            </tr>
            <tr>
                <th>Width</th>
                <td>{{ dom.width }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ dom.height }}</td>
            </tr>
            <tr>
                <th>File_type</th>
                <td>{{ dom.fileType }}</td>
            </tr>
            <tr>
                <th>File_size</th>
                <td>{{ dom.fileSize }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dom_index') }}">back to list</a>

    <a href="{{ path('dom_edit', {'id': dom.id}) }}">edit</a>

    {{ include('dom/_delete_form.html.twig') }}
{% endblock %}
